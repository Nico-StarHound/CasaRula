SUPABASE_URL=https://ryjnwzkrsodgadvqucqa.supabase.co
SUPABASE_SERVICE_ROLE_KEY=
RESTAURANT_ID=

# Printer addresses (per type). Daemon will use printers table from DB,
# but you can override here for local testing.
PRINTER_COCINA_IP=
PRINTER_BARRA_IP=
PRINTER_CAJA_IP=
PRINTER_PORT=9100

# Set to "true" to log ESC/POS bytes to console instead of sending to printer.
DRY_RUN=false
